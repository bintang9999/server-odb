version: '3.8'

services:
  server-odb:
    build: .
    container_name: server-odb
    ports:
      - "8080:8080"
    volumes:
      - odb-data:/app/data
    restart: unless-stopped

volumes:
  odb-data:
